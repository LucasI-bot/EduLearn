<h1><%= @lecture.title %></h1>
<% @lecture.contents.each do |content| %>
    <% case content.content_type %>
    <% when "text_img" %>
        <div class="text-img-div">
            <%= content.text.html_safe %>
        </div>
    <% when "video" %>
        <div class="video-div">
            <video width="100%" height="auto" controls>
                <source src="<%= rails_blob_path(content.video) %>" type="<%= content.video.content_type %>">
                Your browser does not support the video tag.
            </video>
        </div>
    <% when "pdf" %>
        <div class="pdf-div">
            <iframe src="<%= rails_blob_path(content.pdf) %>" width="100%" height="600px" frameborder="0"></iframe>
        </div>
    <% end %>
<% end %>